{
    "version": 2,
    "builds": [{
        "src": "public/**/*.php",
        "use": "@vercel/static-build"
    }],
    "routes": [{
            "src": "/api/(.*)",
            "dest": "/api/$1"
        },
        {
            "src": "/(.*)",
            "dest": "/public/$1"
        }
    ]
}